{
  "permissions": {
    "allow": [
      "Bash(python3 -m venv .venv)",
      "Bash(.venv/bin/pip install *)",
      "Bash(.venv/bin/pytest -v)",
      "Bash(.venv/bin/pytest -q)",
      "Bash(.venv/bin/python -c \"from main import app; print\\('routes:', [r.path for r in app.routes]\\)\")",
      "Bash(.venv/bin/python -c ' *)"
    ]
  }
}
